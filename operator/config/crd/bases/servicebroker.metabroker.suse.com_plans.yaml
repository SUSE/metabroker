
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: plans.servicebroker.metabroker.suse.com
spec:
  group: servicebroker.metabroker.suse.com
  names:
    kind: Plan
    listKind: PlanList
    plural: plans
    singular: plan
  scope: Namespaced
  validation:
    openAPIV3Schema:
      description: Plan is the Schema for the plans API.
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: PlanSpec defines the Service Plan spec.
          properties:
            binding:
              description: PlanBindingSpec defines the Binding spec for a Plan.
              properties:
                credentials:
                  description: PlanBindingCredentialsSpec defines the Credentials
                    spec for a Binding.
                  properties:
                    script:
                      description: PlanBindingCredentialsScriptSpec defines the Script
                        spec for a Binding Credential.
                      properties:
                        implementation:
                          type: string
                        type:
                          type: string
                      type: object
                  type: object
              type: object
            description:
              description: A description for the Plan.
              type: string
            id:
              description: A unique ID for the Plan.
              type: string
            offering:
              description: The name of the Offering this Plan belongs to.
              type: string
            provisioning:
              description: PlanProvisioningSpec defines the Provisioning spec for
                a Plan.
              properties:
                chart:
                  description: PlanProvisioningChartSpec defines the Chart spec for
                    a Provisioning.
                  properties:
                    url:
                      type: string
                  type: object
                values:
                  description: PlanProvisioningValuesSpec defines the Values spec
                    for a Provisioning. It includes the JSON schema for validation,
                    the default values that can be overriden by the user, and the
                    static values that enforce plan-specific configuration.
                  properties:
                    default:
                      type: string
                    schema:
                      type: string
                    static:
                      type: string
                  type: object
              type: object
          type: object
        status:
          description: PlanStatus defines the observed state of Plan.
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
